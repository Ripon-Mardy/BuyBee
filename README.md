## BuyBee

## How to run the project 
First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```


## 🛠 Tech Stack

**Frontend**
- Next.js (App Router)
- TypeScript
- Tailwind CSS
- Shadcn UI


##  Access Pages

The following routes are not protected easily can access

- `http://localhost:3000/cart` (cart page)
- `http://localhost:3000/checkout` (checkout page) 
- `http://localhost:3000/contact` (contact page)
- `http://localhost:3000/product-details` (product details page) 
- `http://localhost:3000/sign-in` (sign-in page) 
- `http://localhost:3000/sign-up` (sign-up page) 


